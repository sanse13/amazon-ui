export interface Product {
  name: string;
  price: number;
  description: string;
  imageRef: string;
}

export interface AccessToken {
  accessToken: string;
}

export interface User {
  name: string;
  email: string;
}
